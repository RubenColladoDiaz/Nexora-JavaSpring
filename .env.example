# Copia este fichero a .env y rellena los valores.
# El .env real NO se sube a git.

# Clave de firma de los JWT. Minimo 32 bytes para HS256.
# Genera una nueva con: openssl rand -base64 48
JWT_SECRET=
